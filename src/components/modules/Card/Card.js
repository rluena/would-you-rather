import styled from "styled-components";

const Card = styled.css`
    background-color: ${props => props.theme.color.background.card.default}
`;

Card. = styled.css`
  width: 550px;
  height: 245px;
`;

export default Card;
